# Default Locust configuration for local interactive runs.
# Override any value via CLI flags or environment variables.
#
# Usage: uv run locust  (picks up this file automatically)

host = http://localhost:8000
locustfile = tests/stress/locustfile.py

# Interactive web UI defaults (overridden when --headless is passed)
web-port = 8089

# Sensible local defaults — override in CI with explicit flags
users = 20
spawn-rate = 5
